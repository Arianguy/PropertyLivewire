import "./bootstrap";
import { Livewire } from "../../vendor/livewire/livewire/dist/livewire.esm";
// The 'livewire-file-uploads' package doesn't exist, removing it
// Livewire v3 has built-in file upload functionality

// Initialize Livewire
Livewire.start();
